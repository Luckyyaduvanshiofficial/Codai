# CodaiPro (Codai Pro) Dependencies
# The Python side is a lightweight controller: it serves the UI, proxies to
# llama-server, and manages the engine process. All AI inference happens in
# the llama-server binary (see engine/ folder and README).

psutil>=5.9.0
